"""Bridges to parametric-CAD and mesh-processing ecosystems.

Each submodule exposes one or two free functions (``from_*`` /
``to_*``) that translate between a peer library's native type and a
PyVista dataset. All imports are lazy.
"""
